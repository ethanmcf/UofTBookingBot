## UofTBookingBot

Bot that auto signs you up for uoft drop in activities

# Instilation

Create venv with python3 and pip3. Run the following commands:
python3 -m venv .venv
source .venv/bin/activate
pip3 install -U selenium
